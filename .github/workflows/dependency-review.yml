name: Dependency Review

on:
  pull_request:
    branches: [main, develop]

permissions:
  contents: read
  pull-requests: write

jobs:
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          # Fail on high or critical severity vulnerabilities
          fail-on-severity: high

          # can only specify allow or deny, not both
          # Allow specific licenses (common in JavaScript ecosystem)
#          allow-licenses: MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, 0BSD, Unlicense, CC0-1.0, WTFPL, Python-2.0, Zlib

          # Only deny truly problematic licenses
          deny-licenses: GPL-3.0-only, AGPL-3.0

          # Comment on PR with results
          comment-summary-in-pr: always

          # Retry configuration
          retry-on-snapshot-warnings: true
          retry-on-snapshot-warnings-timeout: 120
